appId: com.treeto.app
productName: Treeto
directories:
  output: release
  buildResources: resources
icon: resources/icon.icns

files:
  - dist/**/*
  - package.json

mac:
  category: public.app-category.productivity
  target:
    - target: dmg
      arch: [arm64]
  # Ad-hoc sign when no certificate is configured.
  # Users see "unidentified developer" (right-click > Open to bypass)
  # instead of "damaged" which is unrecoverable without xattr.
  # In CI, CSC_LINK env var overrides this with a real certificate.
  identity: null
  notarize: false
  extraResources:
    - from: node_modules/audiotee/bin/audiotee
      to: audiotee
    - from: resources/.env.production
      to: .env
  extendInfo:
    NSMicrophoneUsageDescription: "Treeto needs microphone access to transcribe your voice during meetings."
    NSAudioCaptureUsageDescription: "Treeto needs system audio access to transcribe meeting participants."
    NSScreenCaptureUsageDescription: "Treeto may need screen recording permission for audio capture."
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.plist

dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications

win:
  target:
    - target: nsis
      arch: [x64]
  # FFmpeg is required for Windows audio capture
  # Users should install FFmpeg or bundle it manually
  # extraResources:
  #   - from: resources/win/ffmpeg.exe
  #     to: ffmpeg.exe

linux:
  target:
    - target: AppImage
      arch: [x64]
  category: AudioVideo
  # Linux requires PipeWire for system audio capture
  # pw-record must be available in PATH (part of pipewire package)
