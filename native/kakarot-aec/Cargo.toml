[package]
name = "kakarot-aec"
version = "0.1.0"
edition = "2021"
description = "Acoustic Echo Cancellation native module using SpeexDSP"

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = "1.0"
aec-rs = "1.0"
once_cell = "1.19"

[profile.release]
opt-level = 3
lto = true
